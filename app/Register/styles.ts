import {TextStyle, ViewStyle} from 'react-native';

export const countryAndNumberView = (): ViewStyle => ({
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
  });